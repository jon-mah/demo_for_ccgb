usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
usage: plot_likelihood.py [-h] [--mask_singletons] [--mask_doubletons]
                          syn_input_sfs input_shape input_scale outprefix

Given the number of individuals in population one and two, this script outputs
a `*pops_file.txt` in the format specified for use by the python package,
`easySFS.py`.

positional arguments:
  syn_input_sfs      Synonynomous site-frequency spectrum from which the
                     demographic parameters should be inferred.
  input_shape        The shape parameter for a gamma distribution.
  input_scale        The scale parameter for a gamma distribution.
  outprefix          The file prefix for the output files.

options:
  -h, --help         show this help message and exit
  --mask_singletons  Boolean flag for masking singlestons in Spectrum.
                     (default: False)
  --mask_doubletons  Boolean flag for masking doublestons in Spectrum.
                     (default: False)
